/**
 * Code for the interface shoot, both the player and the enemyBoss can shoot
 * written by
 * @xulin2
 */
public interface Shoot {
    boolean isShootFireBall();
    void setShootFireBall(boolean shootFireBall);
}
